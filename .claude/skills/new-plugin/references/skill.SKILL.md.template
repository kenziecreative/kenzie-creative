---
name: {{NAME}}
description: This skill should be used when the user asks to <FILL IN 2-3 specific trigger phrases, e.g. "do X", "start Y">. {{DESCRIPTION}}
allowed-tools: Read, Write, Edit, Glob, Grep
---

# {{NAME}} — <one-line title>

<!-- The skill logic, written as imperative instructions FOR Claude (not prose to the
     user). Keep the core under ~2000 words; move long reference material into a
     references/ subfolder beside this file and link to it so Claude knows it exists.

     Frontmatter notes:
     - description MUST be third-person trigger-phrase form, and must NOT contain a
       colon-space (": ") in the plain scalar — that silently breaks YAML. Use " — " or
       ",". Keep triggers SPECIFIC to this plugin's domain so it doesn't auto-fire on
       generic phrases.
     - add `model:` (sonnet|opus) to override the default; add
       `disable-model-invocation: true` for destructive/setup skills that should only run
       on an explicit command. -->
