# Maintainer-only notes — never distributed to users
maintainer-notes.md

# Local Claude Code state for this repo — not for distribution
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Local agent/CLI tooling — not part of the marketplace
.agents/
.codex/

# Per-deployment artifacts live in the user's Cowork project, not in this plugin repo.
# Anchored to repo root so the shipped templates/CLAUDE.md is NOT ignored.
# (Root /CLAUDE.md is intentionally tracked — it's the pointer to AGENTS.md, not a deployment artifact.)
/briefs/
/ledger.json
/candidates.json
/sources/

# eval/ run artifacts — captures + scorecards are regenerated, not committed.
eval/**/_eval/
__pycache__/

# Blind-review working notes — candid defect lists of live products; local-only, never distributed
dev/blind-reviews/

# Convergence build execution layer — local-only working docs, same sensitivity as blind-reviews
dev/convergence/
