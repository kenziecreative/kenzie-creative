---
description: Setup Stage 2 — Horizon Mapping. Name your now/next/future and capture the vision.
allowed-tools: Read, Write, Edit, Glob, Grep
---

Run the **Horizons** stage (Setup Stage 2) of the goal-setting method.

Use the `goal-setting-setup-stage` skill for the **horizons** stage and follow its steps
exactly. It reads `${CLAUDE_PLUGIN_ROOT}/reference/stages/02-horizons.md`, applies the Three
Time Horizons framework calibrated to the user's industry velocity, captures a Horizon 3
vision (and the 70/25/5 time distribution if known), and writes the HorizonSet into
`goals/vision.md` before advancing the loop.

It builds on the Direction from Orient — if Orient isn't done, the skill will point the user
there first.
