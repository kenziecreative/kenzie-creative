---
description: How Blueprint works and where to start — the three jobs (discover, quick capture, deep capture) and which one you need
allowed-tools: Read, Write, Edit, Glob, Grep
---

Run the `blueprint-guide` skill and follow it.

Give the user a short orientation to Blueprint's three jobs — discover, quick capture, deep capture —
and route them to the right entry point based on whether they can already name a process they want to
document.
