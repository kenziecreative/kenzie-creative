---
description: Model a process that doesn't exist yet — propose an intended process, grounded in your real constraints and nearest analog, as a designed Blueprint (proposed, not yet run)
allowed-tools: Read, Write, Edit, Glob, Grep
---

Run the `blueprint-design` skill and follow its steps exactly.

Help the user model a process they've been handed but no one runs yet. Propose a candidate flow built
only from their stated goal/constraints and their nearest analogous process — never generic
best-practice — and write a designed Blueprint (proposed, not yet run) with conservative ratings and a
plan to capture it from reality once it runs. If the user already runs the process, this is a capture,
not a design — point them at `/blueprint:capture` instead.
