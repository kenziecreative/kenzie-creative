# Blueprint — deployment config

<!-- This file is installed into the USER's project by the plugin. Blueprint needs no
     required configuration — captures run anywhere and save to blueprints/ by default.
     The field below is optional; delete this file entirely if you don't need it and the
     plugin still works. -->

## Configuration

- blueprints_dir: blueprints/    # where /blueprint:capture writes finished Blueprints (relative to project root)
<!-- Defaults to blueprints/ in the working directory if this file is absent. Nothing else
     about the plugin's behavior is configurable. -->
