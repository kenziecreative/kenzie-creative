# Backstage Convergence Plan

**Status:** LIVING DOCUMENT — updated as patterns prove out in Brand Compass testing. Check the changelog at the bottom and the reference-implementation version before applying anything.
**Last updated:** 2026-07-04
**Owner:** Kelsey. Maintainer agents for this marketplace execute from this plan; they do not invent alternatives to it.

## What This Is

Four Kenzie plugins run the same shape of work — long, multi-session advisory engagements with deep client inputs: **brand-compass** (premium, separate marketplace), and this marketplace's **researcher**, **strategist**, and **goal-setting**. A 2026-07-03 audit scored all four against the Backstage framework (eleven elements, four rooms). Findings: each plugin is strong where its domain forced it and weak where a sibling already solved the problem. This plan converges all four to consistently designed, consistently executed engagement architecture.

**Doctrine (the why and what):** `~/Documents/Claude/Projects/AI Operations/frameworks/backstage.md` — read it before making changes. Do not restate it in plugin content; derive from it.

**Reference implementation (the how, proven in live testing):** `/Users/kelseyruger/Projects/_shared/core-brandcompass-marketplace/plugins/brand-compass` — as of **v3.6.2**, it holds all eleven elements plus the commercial-grade layer added 2026-07-03/04 (register, evidence, artifact, and provenance patterns below). When this plan says "port the Brand Compass pattern," read the actual skill text there rather than working from this summary:

| Pattern | Where it landed | Proven in |
|---|---|---|
| Posture & response register (3 failure modes, 6 register rules, register audit) | `skills/shared/orchestration.md` § Posture & Response Register | 3.3.0 |
| Resume restores stance + engagement-mode grounding in every phase skill | `skills/brand-compass-resume/SKILL.md`, Step 1 blocks in phase skills | 3.3.2 |
| Working Read (hypotheses, mid-thought continuity) | STATE template Client Dynamic > Working Read | 3.3.3 |
| Status lifecycle (Open/Validated/Challenged + would-validate/would-challenge) + anti-contamination rule | STATE template; resume Step 3 | 3.3.4 |
| Session debrief at contact boundaries + Backstage Tasks (private prep list) | `skills/brand-compass-save-state/SKILL.md`; resume executes tasks silently | 3.4.0 |
| Readouts are delivery, not playback + write findings for the non-expert | orchestration § Delivery | 3.4.9-3.5.1 |
| Evidence Discipline for analysis agents (declare corpus; verdicts corpus-bound; "not found" never becomes "invented") | orchestration § Evidence Discipline + 4 analysis agents | 3.5.1 |
| Provenance-gated pushback (challenges name their source in the client's material) + Absorbing Corrections | orchestration § Strategic Pushback | 3.5.2 |
| Metaphors travel, coinages tax (per-turn density; coined phrases must pay for themselves) | orchestration § Delivery | 3.5.3 |
| Reference-don't-restate + orchestration preflights + build-time doctrine-drift lint | phase skills point at doctrine, never copy it; `eval/lib/lint-doctrine-drift.mjs` (stale phrases, missing preflights, deleted referenced sections) | 3.5.4 / 3.5.8 |
| Agent-artifact design register (identity is structure, not tokens; COPY the shipped template, never compose from tokens; render-verify before announcing; on Rebuild, the old brand is evidence, never a rendering style) | `skills/shared/brand-studio-register.md` + `assets/worksheet-template.html` | 3.5.5 / 3.5.7 |
| Workspace markdown grammar (emphasis is information rank, not vocal stress; backstage caveats in Working Notes, never inline) | brand-studio-register § Markdown grammar; pointers from orchestration + 6 writing agents | 3.5.6 |
| Turn architecture — the translation layer (read leads; news beats material; one arc per turn; corrections change shape not substance; read the lean; client-reply compression as gauge) | orchestration § Posture & Response Register | 3.5.9 |
| Deliverables are standing documents (no self-narration, no engagement archaeology, one stance, every principle operational, executable cold) | orchestration § Delivery | 3.5.9 |
| Voice suite + three-layer voice model (generic AI-tell bans → fingerprint unbans with scope/quote/ceiling → idea fidelity outranks both) + anti-formula (enforcement by measurement, never writer self-attestation) | `skills/shared/ai-tells.md`, `agents/voice-encoder.md`, phase-6 pipeline, exemplars | 3.6.0-3.6.1 |
| Claim provenance (every number/testimonial traces to client-confirmed discovery facts; testimonials verbatim-and-permissioned or omitted) + surface-contract integrity (every manifest/dashboard/export list tracks the real deliverable set; counts dynamic) | document-assembler, brand-verifier Checks 6E/6F, OutputPage, export skill | 3.6.2 |

**Pattern status legend used below:** `PROVEN` = shipped in Brand Compass and exercised in live engagement testing. `SHIPPED-UNTESTED` = in Brand Compass 3.4.0 but not yet through a live session. `PROPOSED` = designed here, not yet implemented anywhere. As Brand Compass testing continues, expect statuses (and occasionally the patterns themselves) to change — that is why this document is living.

Current statuses (2026-07-04): posture/register, resume-restores-stance, engagement-mode grounding, Working Read + status lifecycle + anti-contamination, session debrief + Backstage Tasks = PROVEN (exercised across the live engagement's Phase 4-6 sessions; defects found were register-level and are encoded in 3.5.1-3.5.9 with transcript-drawn bad→good pairs). Readouts/findings, Evidence Discipline, provenance-gated pushback, coinage density, reference-don't-restate + drift lint, artifact register + render-verify, markdown grammar = PROVEN (each originated as a live failure, was fixed, and re-exercised or lint-enforced). Turn architecture, standing-document register, the voice suite + three-layer model + anti-formula, claim provenance + surface contracts = SHIPPED-UNTESTED — encoded from live findings and two external cold audits, awaiting the Phase 6 redo as their first live exercise.

## Rules for Maintainer Agents

1. **Respect per-plugin design intent.** Strategist deliberately has no evidence layer and advisory-not-blocking gates (its AGENTS.md says so). Goal-setting deliberately has no outputs gate. Do not "fix" deliberate omissions — converge the engagement architecture, not the domain design.
2. **Adapt language, not structure.** Each plugin keeps its own voice (research partner / strategy advisor / chief-of-staff). The structure — which files exist, when rituals fire, what resume does — converges exactly.
3. **Bump the patch version on every shipped change** and never install/update plugins programmatically on Kelsey's machine — push to the repo; he updates through the interface.
4. **When a pattern here conflicts with what you find in Brand Compass at head, Brand Compass wins** — then flag the drift so this document gets updated.
5. **Verify before you ship.** Confirm the edit actually landed in the file (grep for it) before committing, and never reuse a published version number — if a version went out wrong or empty, the fix gets the next number and a changelog note saying which version to skip. (Origin: Brand Compass 3.4.1 shipped empty when an edit script failed silently.)
6. **Doctrine lives once; skills point at it.** Never copy a doctrine passage into a phase/step skill — the copy goes stale the first time the doctrine moves (Brand Compass shipped three releases that updated orchestration while six phase files kept superseded text). Pair the pointer discipline with a build-time drift lint (stale retired phrases, missing preflights, referenced sections that vanished) that BLOCKS release. When a doctrine change retires wording, add the retired phrase to the lint in the same commit.
7. **Never judge a visual artifact from its source.** Token/spec compliance does not equal identity — a page can use every correct color and font and read as a different product, because identity is carried by structure (chrome, scale, register). Any claim about how something LOOKS requires rendering it and looking, ideally beside a reference surface. Ship visual-output plugins with a copyable template asset, never a "use these tokens" instruction.
8. **Approvals are per-change-set.** A run of "ship it" answers is not a standing license; a feedback handoff ("here's the transcript/notes") means "bring me the proposed change set," not "apply it." The one unreviewed ship of the two days (3.5.7) is also the one that deleted a doctrine section other files referenced.
9. **Adapting the machinery-backstage rule (element 10, rule 7): the word list does not port — the distinction does.** What counts as forbidden machinery differs per plugin because each product's *public interface* differs. In researcher, "audit," confidence tiers, and the claim graph are user-facing product vocabulary — legitimate to say. In strategist, the stage names are the product. In goal-setting, cadences and the three-goal rule are the product. The durable rule: **words in the product's public interface (commands the user runs, concepts the user was taught) are fine; internal plumbing (state files, version reconciliation, calibration notes, skill/step names, "rebuilding context") is backstage.** Build each plugin's tripwire list from ITS plumbing, not Brand Compass's.

## How a Pattern Graduates (SHIPPED-UNTESTED → PROVEN)

Skill-text review does not graduate a pattern. The test is a **live engagement with Kelsey operating as the user**, watching for the gap between "does the right thing" and "feels like the advisor doing it" — the register defects (sycophancy, reset-amnesia, machinery narration) all passed text review and failed live use. A pattern is PROVEN when it has been exercised in a real session and any defects found were encoded back with bad→good pairs drawn from the actual transcript. When you port a pattern, it enters that plugin as SHIPPED-UNTESTED regardless of its Brand Compass status — the adaptation can introduce its own defects.

## Per-Plugin Changes

### researcher (v1.4.1) — strongest ledger/backstage, one real gap

Audit: 8 Present / 3 Partial. The plugin is a hardened superset of the gold-master (hook-enforced gates). Priorities:

1. **[HIGH] Turn-level posture & register (element 10).** The evidence layer resists convergence brilliantly; nothing stops the agent from converging on the *user's* preferred conclusion conversationally. Port the Brand Compass Posture & Response Register section (adapted to research voice — the "client" is a research partner, pushback targets premature certainty and preferred-conclusion steering). Add a register check to the phase-debrief self-audit. `PROVEN` pattern.
2. **[MED] Mid-phase session debrief (element 6).** Phase debrief fires only at Verify; sessions end mid-Collect. Add a session-boundary debrief (the Brand Compass save-state Part 2 shape): in-flight reasoning into `commonplace.md` or a Working Read block, position into STATE.md. `SHIPPED-UNTESTED` pattern — wait for Brand Compass test results before porting, or port behind its own patch version.
3. **[LOW] Backstage tasks (element 7).** Private agent prep queue ("figure X looked shaky — re-check next phase"), distinct from user-facing Next Action and from `notes-to-self.md` (user capture). `SHIPPED-UNTESTED`.

### strategist — strongest conduct, thin backstage/return

Audit: Present 1,5,10,11 / Partial 2,6,8 / Absent 3(by design),4,7,9. Priorities:

1. **[HIGH, one line] Anti-contamination / file primacy (element 9).** Add to the stage engine and progress skill: "STATE.md and brief.md are source of truth; if conversation memory or a compaction summary disagrees, the files win." `PROVEN` pattern, trivial diff.
2. **[HIGH] Working Read (element 4).** Add `## Working Read` to STATE.md (hypothesis — status Open/Validated/Challenged — would-validate/would-challenge — which stage tests it). The advisor forms convictions mid-loop ("suspect the churn framing is wrong; Analyse will tell") with nowhere to hold them. Give the Self-Audit's friction half something concrete to check. `SHIPPED-UNTESTED` in Brand Compass; the strategist's Self-Audit makes it a natural fit.
3. **[MED] Session-boundary debrief + resume ritual (elements 6+8).** Self-Audit fires at stage close; sessions end mid-stage. Add a pause debrief (where the thinking is, the live question, what to pick up) and make resumption *reconstruct the head* — re-adopt Working Dynamic AND Working Read — rather than the read-only `progress` dashboard. `SHIPPED-UNTESTED`.
4. **[LOW] Backstage tasks (element 7).** `SHIPPED-UNTESTED`.

### goal-setting — return is naked; the product promise depends on it

Audit: Present 1,10,11 / Partial 2,3,5,6,8 / Absent 4,7,9. This is the months-long product with *no session-start protocol at all*. Priorities:

1. **[HIGH] The return (elements 8+9).** Add a session-start behavior (templates/CLAUDE.md instruction and/or hook): read STATE.md, re-adopt `## Working Dynamic` calibration (currently only setup stages read it — pulse/review never do; fix that too), and state the trust order: files over chat memory and compaction summaries. `PROVEN` pattern. Highest leverage change in this entire plan relative to product promise.
2. **[HIGH] Track the hypothesis the system already generates (elements 4+2).** Monthly goal-wrong vs execution-wrong calls and pressure-test findings are multi-month hypotheses currently flattened into journal lines. Give them Working Read entries with status lifecycle + light provenance (which pulse/review data triggered them), so "flagged three months running" is visible instead of re-discovered cold. Adapted `SHIPPED-UNTESTED` pattern — this is the highest-product-fit adaptation in the plan.
3. **[MED] Session pause debrief + backstage tasks (elements 6+7).** Quarterly reviews span multiple sessions; add an in-progress marker + next-session prep stub distinct from user-facing Next Action. `SHIPPED-UNTESTED`.

## Deliverable Convergence (separate workstream — design, not port)

Brand Compass has the most mature final-deliverable system: three bundles shaped per consumer (human client package with designed HTML specimens; agent skill bundle so other AI tools consume the output; implementation kit), plus a verification gate before export and a document-assembler that designs rather than templates. The other three plugins end in markdown files of varying ambition.

This does NOT port mechanically — the specimen/design system is brand-specific. What converges is the *pattern*, and each plugin needs its own deliverable **definition** against it:

| Pattern element | Question each plugin must answer |
|---|---|
| Consumer-shaped bundles | Who consumes the output? (human reader / downstream AI agent / implementer) — one bundle per real consumer, not one-size exports |
| Designed final artifact | What does "feels made for this engagement, not templated" mean here? (researcher: the report; strategist: the strategy brief; goal-setting: the goal system + review record) |
| Verification gate | What must be true before export? (placeholder scan, cross-document consistency, claim-audit already exists in researcher) |
| Agent-consumable output | What would another AI tool need to act on this engagement's result? (researcher's claim-graph is ahead here; strategist/goal-setting have nothing) |

Status: `PROPOSED` — requires per-plugin design sessions with Kelsey, not maintenance-agent execution. Logged here so the workstream isn't lost.

## Authoring Lessons (apply to every port)

1. **Skill section headings become the agent's spoken vocabulary** — write "Reconcile Versions" as a step name and the agent will tell the user it is "reconciling versions." Internal step names are for the agent; each skill doing multi-step internal work needs an explicit narration firewall ("these steps run silently; the user sees at most one natural line"). See Brand Compass resume (3.4.3).
2. **Doctrine phrasing becomes the agent's pet phrases** — distinctive words in instructions and example quotes get adopted as tics ("load-bearing" was absorbed from the posture doctrine's own examples and repeated constantly in live sessions). Write instructions in plain language, vary the example quotes, and port the pet-phrase guard in the register audit (Brand Compass 3.4.4). When adapting examples to a plugin's voice, don't hand it a new signature phrase.
3. **Cross-artifact filename contracts must be explicit on the writer side and tolerant on the reader side.** If any rendering surface (dashboard, report generator, deliverable assembler) maps files by name, the skill that writes the file must state the exact filename ("use this exact filename — the dashboard reads this path"), and the reader must still glob-fallback so a varied name degrades gracefully instead of blanking. A glob in the doctrine plus hard-coded names in the reader means the agent improvises names and the surface silently half-works (Brand Compass 3.4.5 — caught in live testing, invisible in fixture testing because the fixture never contained the files in question; make fixtures exercise every file contract).
4. **Rules against formula must not become a formula.** Anti-formula doctrine addressed to a writing agent decays into a pre-flight checklist it satisfies mechanically. Encode prohibitions and ceilings in the instructions, but put the ENFORCEMENT in measurement (verifier density/predictability checks, eval judge dimensions) — never in writer self-attestation. Frequencies are always ceilings or tells, never quotas; a "move presence" check in a verifier is the same disease inverted (Brand Compass 3.6.0 inverted exactly such a check in its own verifier).
5. **External cold audits are a repeatable QA instrument — with a verification pass.** Two Codex reviews (2026-07-04) each found real, shippable defects the inside view missed (doctrine/skill drift; claim-provenance gap), at roughly a 60-70% confirmation rate — so every finding gets verified against the actual files and rendered surfaces before anything ships, and the false positives (it can't see the eval pack or runtime design intent) get documented in the commit. Give the auditor the calibration excerpts as ground truth and demand file:line citations.
6. **The register has two failure poles and corrections swing between them.** Walls of text and stripped bullet-skeletons are one missing capability (translation) seen from both sides; a "too long" correction produces the opposite pole unless doctrine says corrections change the SHAPE, not the substance. And the first attempt at a prescribed register shape will be worn as visible labeled sections — the eval rubric must penalize the visible scaffold or the fix trains a third failure mode (Brand Compass 3.5.9 + calibration set 358).

## Changelog

- **2026-07-04 (Brand Compass 3.4.9 → 3.6.2, ten releases; two live sessions + two external cold audits)** — Reference implementation bumped to 3.6.2. Twelve pattern rows added (readouts/findings, Evidence Discipline, provenance-gated pushback, coinage density, reference-don't-restate + drift lint, artifact design register + render-verify + template asset, markdown grammar, turn architecture, standing-document register, voice suite + three-layer model + anti-formula, claim provenance + surface contracts). Statuses: the 3.3.x-3.4.0 pattern set graduated to PROVEN via the live engagement's Phase 4-6 sessions; the 2026-07-04 set enters SHIPPED-UNTESTED pending the Phase 6 redo. Rules 6-8 added (doctrine-lives-once + drift lint; render-and-look for visual claims; approvals are per-change-set). Authoring lessons 4-6 added (anti-formula enforcement lives in measurement; external cold audits with a verification pass; the two register poles + template-wearing third mode). Deliverable-convergence workstream note: Brand Compass's voice suite (3.6.0) is now the reference answer to the "agent-consumable output" row — a client-owned writing-rules file the packager copies verbatim, rather than a bundle derived by translation.
- **2026-07-03 (session-2 transcript review, Brand Compass 3.4.5-3.4.6)** — Three additions, all portable: (1) **Additive state-schema migration in resume** — state files are created from the template at engagement start and never rescaffolded, so template evolution silently never reaches running engagements; resume now adds missing sections (never removes/rewrites). Highest relevance: goal-setting, whose state lives for months. (2) Dashboard/deliverable refresh joins the incorporate-agent-results protocol, not just phase close. (3) Exact-key debugging rule for any file-contract troubleshooting (substring grep masked a filename mismatch and produced a confident wrong diagnosis to the client). Also: filename-contract fix (authoring lesson 3) shipped as 3.4.5. Transcript review validated the register fixes — resume narration, pet phrases, grade-before-validate all confirmed as the real failure modes in live use; grade-own-move-down (agent downgrading its own earlier take in front of the client) confirmed as the register done RIGHT — preserve it when porting element 10.
- **2026-07-03 (later still)** — Resume narration firewall + rule 7 tripwire broadened (Brand Compass 3.4.3). Authoring lesson section added. Rules 5-6 added (verify-before-ship; machinery-vs-product-vocabulary adaptation). "How a Pattern Graduates" section added — ported patterns re-enter as SHIPPED-UNTESTED.
- **2026-07-03 (later)** — Register rule 7 added to the posture pattern (Brand Compass 3.4.2, from live testing): "keep the machinery backstage" — never narrate modes/skills/state-files/protocols to the user; do the behavior, don't announce its name. Applies verbatim to all three plugins when porting element 10. Note: Brand Compass 3.4.1 shipped empty (tooling slip); reference version for the posture section is 3.4.2.
- **2026-07-03** — Initial plan from the four-system audit. Brand Compass reference implementation at 3.4.0 (all eleven elements). Audit evidence lives in the audit reports (summarized in the per-plugin sections; file:line citations were verified against researcher v1.4.1, strategist and goal-setting at their 2026-07-03 heads).