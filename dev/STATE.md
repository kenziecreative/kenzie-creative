# Work state — kenzie-creative-marketplace

**Last updated:** 2026-07-12 · **Session focus:** Codex pass 2 consumed. Verdict verified (9/9, 0 refuted), triage note filed, **all eight open findings repaired and shipped as v1.1.0.** **The next action is Kelsey's merge-to-main call.**

## Where things stand

- **intelligence-briefing (1.1.0 — "durable collection")** — **SHIPPED on branch `review/intelligence-briefing`**, tag `intelligence-briefing-v1.1.0` (commit `2ac3f4e`), branch + tag pushed. This is the **pass-2 repair release** and it is the **merge candidate** — v1.0.0 should NOT merge (pass 2's verdict was "not trustworthy for unattended briefing," and the README makes the unattended claim). Validation green at both scopes; version prefixes agree across all four surfaces.
  - **Pass-2 verdict:** 1 CLOSED (the action gate holds), 7 PARTIAL, 1 disclosed-open-and-worsened. **All nine citations verified against the live files; 0 refuted; 1 narrowed** (Finding 5 — the real defect was the *predicate scope*, not the epistemology, and Codex's fix was heavier than the defect). **Zero lock collisions** — a first, and evidence the AGENTS.md locks were drawn in the right places.
  - **The organizing insight (and what v1.1.0 fixes):** pass 1 was *the run is the system*; pass 2 was one level in — **collection success was not durable.** The system recorded an obligation, failed it, and lost the failure. A rate-limited cell was disclosed on the morning it failed, then advanced as though it had succeeded. Five of the eight findings were write-only or never-written obligation outcomes. One rule fixes them all: **every mandatory obligation records an outcome, and the outcome gates what the brief may claim.**
  - **Three things we found that Codex missed**, all by grep, all now fixed: `last_status` was read by nobody; `window_start`/`window_end` were read by nobody (so there was effectively *no collection window at all*); and **zero-due was the default state, not a corner case** — an unstaggered weekly matrix on a daily cadence has zero cells due six mornings out of seven, which under the `failed` reading meant *no brief at all* six days a week. That interaction is nobody's finding but ours and it was a first-week product-killer.
  - Full account: `dev/blind-reviews/intelligence-briefing-pass2-2026-07.md` **in the core checkout** (ONE INBOX), and the 1.1.0 CHANGELOG entry.
- **strategist (0.4.1)** — convergence release + pass-2 hardening on main, tagged and pushed. Ported patterns SHIPPED-UNTESTED (no live session yet). Full account: `strategist/CHANGELOG.md`.
- **goal-setting (0.2.1)** — SHIPPED 2026-07-12. Rubric anchors calibrated; nothing open on Kelsey. Next verification step: first golden-set run (`/eval-run --target goal-setting`).
- **researcher (1.4.1)** — releases last (A1). Register port (D1) and evidence architecture (B1) still wait on Kelsey's sessions.
- **eval harness** — two pack follow-ups queued. No eval target exists for intelligence-briefing; one is a v1.x deliverable now that the rebuild has shipped (the spec deliberately excluded it from the build).

## Done this session

- **Pass-2 verdict consumed** per the protocol pipeline. Every citation verified against the live files, both directions; all claimed absences confirmed by grep, not taken on faith. **9/9 hold, 0 refuted, 1 narrowed.**
- **Triage note filed** to the ONE INBOX: `/Users/kelseyruger/Projects/_shared/core-kenzie-marketplace/dev/blind-reviews/intelligence-briefing-pass2-2026-07.md` (146 lines: triage table, the one-disease insight, what Codex missed and overreached on, cross-plugin significance, the repair plan, raw verdict verbatim).
- **v1.1.0 built, validated, tagged, pushed** (`2ac3f4e`) — all eight open findings repaired across six files. Failed obligations became debts; signposts and falsifiers got outcomes; `idle` named; the rotation staggered; per-cell windows; derivative-aware driver accounting; URL dedup; predicate verification; the overflow schema; matrix reconciliation on relevance correction.
- **Ran the field-consumer sweep before tagging** (the STATE lesson from the last session, extended by what pass 2 taught): every new field grepped for a *reader*. It caught four write-only fields of my own before they shipped — the same defect I had just filed against v1.0.0.
- **Ran the full build-spec review standard + a state-machine trace, and wrote the proof trail** (`dev/rebuild/BUILD-REPORT-v1.1.0.md`) — prompted by Kelsey catching that the first pass had run only 4 of the 10 points and written no report, which the spec says means the build has not finished. **All 10 points pass.** The trace (a synthetic deployment, day by day) caught **three defects I had introduced**, all fixed: the stagger colliding with the "never scanned = due" rule (would have collapsed the stagger entirely on run one); **"0 of 0" reachable from the `complete` rendering** even though I'd banned it from `idle` (I locked the state and left the sentence reachable from another one); and `run_window_start` written by a step and read by none — my own rule, violated by me.

## Next steps (in order)

1. **Kelsey: the merge-to-main call.** v1.1.0 is the merge candidate; **v1.0.0 is not** (pass 2: "not trustworthy for unattended briefing"). Installers pull main, so this is the real ship. Two equivalent paths: merge in `core-kenzie-marketplace` (the strategist pattern), or the PR GitHub offers for the pushed branch: https://github.com/kenziecreative/kenzie-creative/pull/new/review/intelligence-briefing
2. **Optional — pass 3 on intelligence-briefing.** The protocol's re-attack cycle has now caught defects at both passes, and on Brand Compass a pass-2 re-attack caught something a *repair itself introduced*. v1.1.0 is a large state-machine change made in one session with no independent check — the same caveat v1.0.0 carried into pass 2, and pass 2 found eight things. **Not free, and Kelsey's call**, but this is the highest-value review target in the marketplace right now.
3. **Build the intelligence-briefing eval pack** (golden set + rubric) — unblocked, and v1.1.0 gives it much sharper things to grade: health line always present; degraded ≠ quiet ≠ idle; a failed falsifier never renders as "nothing surfaced"; a failed cell is still due next run; no padding. Grade behavior, never recommendation correctness.
4. **Kelsey: strategist STOP package** — `dev/convergence/review-queue/strategist-rubric-anchors.md`, plus two eval-pack follow-ups (`single_stage_advance` must accept 0.4.1's honest statuses; pin `adv-preference-over-evidence`'s end-state).
5. **goal-setting: first golden-set run.** Nothing gates it.

## Open questions / decisions pending

- **Merge to main** — the only thing intelligence-briefing now waits on.
- **Pass-1 Finding 8 (relevance-context staleness cadence) is STILL deliberately open.** Note the distinction, it matters: pass 2 confirmed a *different, new* defect under the same number — a correction the user *does* make never reached the coverage matrix — and **that one is fixed in 1.1.0.** The original item (nothing ever asks whether the mandate still holds) remains unbuilt. Fix shape from pass-1 triage still stands if we want it: record-don't-restrict (a `Context confirmed:` stamp + an age line in the brief past a threshold).
- **v1.1.0's own watch list** — full detail in `dev/rebuild/BUILD-REPORT-v1.1.0.md` §2 and §7. Short version: the stagger formula is naive and can bunch cells on a small matrix; `last_attempted` and `retrieved` have readers but only in the review conversation (thin); requiring `source_opened: true` for `act`/driver-moving observations adds WebFetch calls — watch whether that re-creates the rate-limit pressure the stagger just relieved; and the standing gate lets drivers move in week one while most of the matrix is still unswept (correct, but the rotation line is the only thing disclosing it).
- **Nothing in v1.1.0 has ever run.** Every check is static — greps, validation, a hand-traced state machine. No deployment set up, no scan collected, no brief rendered, no eval pack. Said plainly in BUILD-REPORT §7 and it is the honest reason pass 3 (or a first live run) matters.
- **Build-report watch list** (judgment calls that might prove wrong in use): proposals living in `drivers.json`; export-time-only STEEP with null stored fields; the run-record `reckoning: true` flag; active-only export. See BUILD-REPORT §2.
- Strategist rubric anchors + golden scenario changes: Kelsey-gated, package in the review queue.
- **Strategist pass-3: SKIPPED** (Kelsey, 2026-07-12). Rationale and reopening tripwire in git history (`66fd768`).

## Session knowledge worth keeping

- **The read-by-nobody family has a third form, and it is the one that hides:** not *config read by no step*, and not *schema field written by nobody*, but **a field written by a step and read by no step.** It is perfectly implemented on the write side, so it survives every check that asks "is this wired up?" — and it is pure decoration. Two shipped in v1.0.0 (`last_status`, the run windows) and survived STOP B's targeted sweep of the other two forms. **The sweep to run is: for every field in a schema, grep for a *consumer*; if the only hits are the definition, the writer, and the docs, the behavior it was supposed to enforce is not happening.** This caught four of my own write-only fields in v1.1.0 before they shipped. It should become a mechanical lint across the marketplace.
- **New family, and it generalizes past this plugin: an obligation without a recorded outcome is theater.** Any step a skill declares *mandatory* must leave a durable trace of whether it succeeded, or every downstream claim that depends on it ("nothing surfaced," "collection current," "coverage complete") is unfalsifiable. This is pass-1's captured-contract-read-by-gate one level up: there a captured *value* was read by no gate; here a mandatory *operation* was recorded by no field.
- **Codex's diagnoses are worth more than its prescriptions.** Confirmed again: 9/9 citations verified, 0 refuted across two passes — but its fixes consistently cost more than the defects require (this time: re-open and re-extract every emitted claim, on a product that was already facing a 100+ search first run). **Take the diagnosis; re-price the remedy.** It is epistemically strong and product-cost blind, and it does not know these are solo-operator conversational tools.
- **Disclosing the locks in the pass-2 prompt worked exactly as intended.** Told the verifier subagent was locked, Codex wrote *"This does not require a verifier subagent"* and routed around it. Zero lock collisions this pass, versus several in pass 1 — which is itself evidence the locks are drawn in the right places. **Keep disclosing them.**
- **Two findings can multiply into a third that neither reviewer sees.** Codex found the no-stagger defect and the zero-due status contradiction as separate items and never combined them. Together they meant a daily brief would produce *nothing at all* six mornings out of seven. **When triaging a finding list, cross the findings against each other before pricing them** — the interaction was more dangerous than any single item on the list.
- **The one-shot build held, but the post-build sweep still earned its keep:** four real gaps survived a spec-faithful build, all in the family pass-1 taught (config read by no step; schema field written by nobody). **On any future one-shot build, run that family sweep before tagging** — it found what the ten-point checklist could not.
- **The worktree path guard blocks Write/Edit outside this repo, including the scratch staging dir.** The ONE INBOX rule still governs; place scratch files via bash (`cat > path <<'EOF'`) — that's how PASS-2-PROMPT.md landed. (Known tension, previously recorded at `969bed5`.)
- **The pass-2 appendix is Codex's RAW verdict only** — never the triage note, which carries internal vocabulary and Kelsey's design decisions the reviewer must not see.
- **Spec-defect patterns worth repeating on future build specs** (all hit this build, all had conservative completions): a record consumed by three sections but defined by none (proposals); a schema example contradicting a lifecycle rule (STEEP populated vs export-time-only); a trigger with no persistence (the reckoning's "30 days since last"); "empty forms" colliding with "setup seeds content." Full list: BUILD-REPORT §3.
- **The June 7 brief remains the most important artifact in this workstream** — the rebuild exists because the machine wrote the intelligence into HTML for the human and kept a shopping list for itself. Never design a rebuild without reading the thing's actual output.
- Prior sessions' knowledge (worktree path guard vs. one-inbox, Codex's heading-offset citation quirk, eval dispatch pattern, drift-lint usage, canon location) is unchanged — see `969bed5` and `66fd768` in git history.

## How to resume

1. Read `AGENTS.md` (orientation), then this file.
2. **intelligence-briefing is done and waiting on one human decision: merge v1.1.0 to main.** Nothing else on it is blocked. If Kelsey wants the extra confidence first, next step 2 (pass 3) is the argument for it.
3. Grounding docs, in order of authority: `intelligence-briefing/AGENTS.md` (the locks — now carrying the 1.1.0 durable-collection invariants), the **1.1.0 CHANGELOG entry** (what changed and why), `dev/blind-reviews/intelligence-briefing-pass2-2026-07.md` **in the core checkout** (the pass-2 triage; the standing record the next audit cites), `dev/rebuild/BUILD-REPORT.md` (the 1.0.0 build's judgment calls), `dev/blind-reviews/intelligence-briefing-pass1-2026-07.md` (why the rebuild exists at all).
4. **Do not re-litigate the state model from the build spec.** `dev/rebuild/intelligence-briefing-v1-build-spec.md` describes 1.0.0 and is now partly superseded: the failed-cell transition, the run statuses, the windows, the driver-attachment rule, and the overflow contract all changed in 1.1.0. The skills and AGENTS.md are the truth; the spec is history.
